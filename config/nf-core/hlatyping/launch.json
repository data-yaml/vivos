{
    "computeEnvId": "{{ computeEnvId }}",
    "pipeline": "https://github.com/{{ pipeline }}",
    "workDir": "{{ bucket }}",
    "revision": "master",
    "configProfiles": [
        "test"
    ],
    "configText": "plugins = ['nf-quilt']"
}
